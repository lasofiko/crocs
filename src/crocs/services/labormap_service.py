from __future__ import annotations

import pandas as pd


def build_hourly_demand(forecast_guests: pd.DataFrame, reqlabor: pd.DataFrame) -> pd.DataFrame:
    raise NotImplementedError("прогноз гостей → потребность по станциям по reqlabor")
